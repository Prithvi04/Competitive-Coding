#include <bits/stdc++.h>
void dis_gift(int arr1[],int arr2[],int size)
{
    set<int> sum;
    for(int i=0;i<size;i++)
    {
        int s=0;
        
    }
    
}
int main()
{
    int n;
    cin>>n;
    for(int i=0;i<n;i++)
    {
        int n1;
        cin>>n1;
        int arr1[n1];
        int arr2[n1];
        for(int i=0;i<arr1.length();i++)
        {
            cin>>arr1[i];
        }
        for(int i=0;i<arr2.length();i++)
        {
            cin>>arr1[i];
        }
        dis_gift(arr1,arr2,n1);
    }
    return 0;
}